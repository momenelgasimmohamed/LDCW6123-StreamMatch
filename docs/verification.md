# Verification and remaining work

Actual local verification: GCC and Clang compiled with strict warnings treated as errors. Both CTest suites passed. Unit tests also passed with Clang address/undefined-behaviour sanitizers enabled. See the unedited text evidence and environment record.

The GitHub Actions workflow is supplied but has NOT run on a remote service. Its presence is not evidence that Windows or macOS tests passed. No remote repository or public URL exists yet.

Initial commits identify ChatGPT-assisted draft development. They do not prove participation by the six students. Each member should make meaningful, attributable contributions after reviewing the code and research, within course policy. Replace the draft Git evidence in the final report with the completed history that retains the initial provenance.

Known limits: 12 fictional titles, no real recommendation training, no Netflix API, no streaming, no database, no persisted preferences, and illustrative age labels. The hard-filter logic is tested but is not a parental-control certification. New catalogue entries should use unique IDs and valid metadata.

Before submission, the group must run the build, tests and live demo on its own machine, verify citations, complete truthful contribution declarations, supply authorized fresh signatures, host the code and video, and obtain required Turnitin reports. No marks or detector result are guaranteed.
